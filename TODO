- before applying another safe mode turn, check if the normal solution would work fine (would reach positive speed) and prevent safe mode
- bumperCenter -> location

better C++:
- explicit constructors
- operator<< instead of toString()
